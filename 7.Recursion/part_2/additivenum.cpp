#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    bool f(string &num, int ind, int num1, int num2){
        
        if(num1 != -1 and num2 != -1){

        }

        
    }
    bool isAdditiveNumber(string num) {
        return f(num, 0, -1, -1);
    }
};

int main() {

    return 0;
}